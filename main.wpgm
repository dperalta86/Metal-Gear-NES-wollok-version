import wollok.game.*

import src.system.system.*


import src.characters.snake.*

program MetalGearNES {
	// Configuración global del juego
	config.load()

	// Cargo nivel (inicia level 0)
	levels.load_level()

	// Inicio juego
	game.start()
}
